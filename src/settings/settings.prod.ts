import { ISettings } from '../app/commons/interfaces/SettingsInterface';

export const SETTINGS: ISettings = {
    urls: {
        api: 'http://localhost:3004',
        login: 'auth/login',
        userInfo: 'auth/userInfo',
        courses: 'courses',
        authors: 'authors'
    }
};
